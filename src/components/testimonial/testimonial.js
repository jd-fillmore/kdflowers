import React from "react"
import { Container, Row, Col } from "reactstrap"

import "./testimonial.scss"

function Testimonial({title, text, author}) {
  return {
    <Container>
    </Container>
  }
}

export default Testimonial
